<!--index.wxml-->
<view class="container">
  	
	<view wx:for-items="{{gameList}}" wx:for-index="idx" wx:key="idx" class="usermotto">
	    <button class="btn1" disabled="{{disabled}}" bindtap="start{{item}}" > {{item}} </button>
	</view>

	<text class="more">更多内容，敬请期待...</text>
</view>
